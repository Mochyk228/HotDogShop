extends Node

signal next
var childrens
var i : int

#func _ready() -> void:
	#children = self.get_children()
	#next.connect(next_client)
#
#func next_client():
	#var next_c = children[i].instantiate()
	#add_child(next_c)
